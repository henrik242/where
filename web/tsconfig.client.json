{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    // Browser-only program: keep the @types/bun and @types/node globals out.
    "types": []
  },
  "include": [
    "src/client/**/*",
    "src/shared/**/*"
  ]
}
