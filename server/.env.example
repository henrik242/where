# Server Environment Variables
# Copy this to .env and fill in your values

# Port to run the server on (default: 3000)
PORT=3000

# HMAC secret key for verifying tracking requests
# Generate a secure random string (e.g., using: openssl rand -base64 32)
# This must match the TRACKING_HMAC_SECRET in the Android app build
TRACKING_HMAC_SECRET=your-secret-key-here

# Optional: God mode key for viewing all tracks
GOD_MODE_KEY=your-god-mode-key-here

