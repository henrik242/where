package no.synth.where.util

actual fun extractFirstFileFromZip(zipData: ByteArray, extension: String): ByteArray? {
    // Deferred for MVP — county borders won't load but app works fine without them
    return null
}
